# 🔧 CHANGELOG - v0.6.2

## 📋 Résumé

Gestion du multi-instance dans la GUI : sélecteur agent et instance pour visualiser individuellement chaque agent en cours d'entraînement.

---

## ✅ 1. Sélecteur Agent / Instance dans le header

### Avant (v0.6.0)
```python
# training_gui.py — La GUI n'affichait que les stats de l'agent 0
# Impossible de voir ce qu'un autre agent faisait
self._data: Dict = {}  # stats globales sans clé agent
```

### Maintenant (v0.6.2)
```python
# training_gui.py — Sélecteur dans le header (Combobox)
# Agent: [0] [1] [2]    Instance: [0] [1]
self._agent_var    = tk.IntVar(value=0)
self._instance_var = tk.IntVar(value=0)

self._agent_combo    = ttk.Combobox(left, textvariable=self._agent_var, ...)
self._instance_combo = ttk.Combobox(left, textvariable=self._instance_var, ...)

# Données stockées par (agent_id, instance_id)
self._data: Dict[tuple, Dict] = {}   # (aid, iid) → stats dict
self._episode_data: Dict[tuple, List] = {}  # (aid, iid) → épisodes
```

---

## ✅ 2. `update_stats()` — Dispatch par clé (agent, instance)

```python
# training_gui.py
def update_stats(self, stats: Dict[str, Any]):
    """Thread-safe : stocke les stats pour (agent_id, instance_id)."""
    aid = int(stats.get('agent_id', 0) or 0)
    iid = int(stats.get('instance_id', stats.get('env_idx', 0)) or 0)
    key = (aid, iid)

    # Enregistre l'agent/instance comme connus → met à jour les Combobox
    if aid not in self._known_agents:
        self._known_agents.append(aid)
        self._known_agents.sort()
    if aid not in self._known_instances:
        self._known_instances[aid] = []
    if iid not in self._known_instances[aid]:
        self._known_instances[aid].append(iid)

    self._data[key] = {**self._data.get(key, self._default_stats()), **stats}
```

---

## ✅ 3. `_refresh_ui()` — Lecture depuis la clé sélectionnée

```python
# training_gui.py — Toutes les métriques lisent depuis le (agent, instance) actif
def _refresh_ui(self):
    key   = (self._sel_agent, self._sel_instance)
    stats = self._data.get(key, self._default_stats())
    # → HP, stamina, reward, zone, monsters... tous de CET agent/instance
```

---

## ✅ 4. Auto-découverte des agents et instances

```python
# training_gui.py — Les Combobox se mettent à jour automatiquement
# quand un nouvel agent commence à envoyer des stats
def _refresh_ui(self):
    if self._agent_combo:
        new_vals = [str(a) for a in self._known_agents]
        if curr_vals != new_vals:
            self._agent_combo['values'] = new_vals  # Ajout en temps réel
```

---

## ✅ 5. `_on_agent_changed()` / `_on_instance_changed()` — Cascades

```python
# training_gui.py — Changer d'agent met à jour la liste d'instances disponibles
def _on_agent_changed(self, _event=None):
    self._sel_agent = int(self._agent_var.get())
    instances = self._known_instances.get(self._sel_agent, [0])
    self._instance_combo['values'] = [str(i) for i in instances]
    self._sel_instance = instances[0]
    self._instance_var.set(self._sel_instance)
    self._last_chart_time = 0.0  # Force redraw des charts
```

---

## ✅ 6. Callbacks — Stats pour toutes les instances secondaires

```python
# train/callbacks.py — GUIUpdateCallback envoie les stats de chaque instance
def gui_update(self) -> bool:
    # Instance principale (idx=0) → déjà traitée
    for idx in range(1, len(all_infos)):
        owner_agent = self._env_to_agent.get(idx, idx)
        self.gui.update_stats(_build_env_stats(
            env_idx=idx, agent_id=owner_agent, ...
        ))
# → Toutes les instances ont leurs données dans self._data

def __init__(self, ..., allocation=None):
    # Reconstruction env_idx → agent_id depuis l'allocation
    self._env_to_agent = {}
    if allocation:
        for aid, insts in allocation.items():
            for iid in insts:
                self._env_to_agent[iid] = aid
```

---

## ✅ 7. `add_episode_data()` — Par agent et instance

```python
# training_gui.py — Les épisodes sont stockés par (agent, instance)
def add_episode_data(self, episode, reward, length, hits=0,
                     agent_id=0, instance_id=0):
    key = (agent_id, instance_id)
    if key not in self._episode_data:
        self._episode_data[key] = []
    self._episode_data[key].append({'ep': episode, 'r': reward, ...})
```

---

## ✅ 8. Forcer redraw des graphiques au changement d'agent

```python
# training_gui.py — Quand on change d'agent/instance, les graphiques se rechargent
def _on_agent_changed(self, _event=None):
    self._last_chart_time = 0.0  # Invalide le cache

def _deferred_chart_redraw(self, delay_ms=150):
    self._last_chart_time = 0.0
    self.window.after(delay_ms, self._force_chart_refresh)
```

---

## 📦 Fichiers Modifiés

| Fichier | Changement |
|---------|-----------|
| `GUI/training_gui.py` | Sélecteur agent/instance, `_data[(aid,iid)]`, `_refresh_ui` ciblé |
| `train/callbacks.py` | `GUIUpdateCallback` envoie stats toutes instances, `_env_to_agent` |

---

## 📊 Impact

### Avant v0.6.2
- Vue unique (agent 0 seulement) en multi-instance
- Impossible de surveiller individuellement chaque agent

### Après v0.6.2
- Dropdown "Agent" + "Instance" dans le header
- Clic → toutes les métriques passent à cet agent/instance
- Les graphiques et la carte 3D se rechargent immédiatement
- Nouveaux agents auto-découverts à la connexion

---

**Version : 0.6.2**

